name: Build IIIF Manifests
on: [push]
permissions:
  contents: write
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Manual Manifest Generation
        run: |
          mkdir -p public/webb/item
          # Copy the image into the public folder
          cp iiif/webb/item/Webb_Union.jpg public/webb/item/Webb_Union.jpg
          
          node -e "
          const fs = require('fs');
          const repoBase = 'https://alleghenydh.github.io/student_iiif_repository';
          
          const manifest = {
            '@context': 'http://iiif.io/api/presentation/3/context.json',
            'id': \`\${repoBase}/webb/index.json\`,
            'type': 'Manifest',
            'label': { 'en': [ 'Webb Union' ] },
            'items': [
              {
                'id': \`\${repoBase}/webb/canvas/1\`,
                'type': 'Canvas',
                'height': 1500,
                'width': 2000,
                'items': [
                  {
                    'id': \`\${repoBase}/webb/page/1\`,
                    'type': 'AnnotationPage',
                    'items': [
                      {
                        'id': \`\${repoBase}/webb/annotation/1\`,
                        'type': 'Annotation',
                        'motivation': 'painting',
                        'body': {
                          'id': \`\${repoBase}/webb/item/Webb_Union.jpg\`,
                          'type': 'Image',
                          'format': 'image/jpeg',
                          'height': 1500,
                          'width': 2000
                        },
                        'target': \`\${repoBase}/webb/canvas/1\`
                      }
                    ]
                  }
                ]
              }
            ]
          };

          fs.writeFileSync('public/webb/index.json', JSON.stringify(manifest, null, 2));
          "
          touch public/.nojekyll

      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: public
          branch: gh-pages
